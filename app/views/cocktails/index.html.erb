<h1>Cocktails</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @cocktails.each do |cocktail| %>
    <tr>
      <td><%= cocktail.name %></td>
      <td><%= link_to 'Show', cocktail %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Cocktail', new_cocktails_path %>
